<Window x:Class="kursach.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:kursach"
        mc:Ignorable="d"
        Title="Резервация" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100*"/>
            <RowDefinition Height="350*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding ElementName=FrmMain, Path=Content.Title}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="50"></TextBlock>
        <Frame x:Name="rezervation" Grid.Row="1" NavigationUIVisibility="Hidden"></Frame>
    </Grid>
</Window>
